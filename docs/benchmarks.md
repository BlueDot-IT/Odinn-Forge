# Current comparative benchmarks

This page is owned by Odinn Forge and generated by the weekly GitHub-hosted
benchmark workflow. The external
[BlueDot agent-benchmarks harness](https://github.com/BlueDot-IT/agent-benchmarks)
owns the cases, deterministic graders, and raw report contract.

The first scheduled publication is pending. Until a complete three-runtime
matrix passes strict comparison and provenance validation, the dated historical
snapshot in the root README remains the last available evidence. The workflow
will replace this page atomically after a successful run; failed or incomplete
runs do not overwrite it.
