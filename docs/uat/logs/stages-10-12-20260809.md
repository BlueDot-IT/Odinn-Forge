# Stages 10–12 proof log

## Scope

- Repository: `BlueDot-IT/Odinn-Forge`
- Branch: `codex/stages-10-12-20260809`
- Feature: durable workflows, authenticated event ingress, and scoped project context.
- Runtime surfaces remain disabled by default and require their explicit configuration flags.

## Exact validation commands and observed results

All local checks were run sequentially. The focused test runner was explicitly
configured with `--test-concurrency=1` and capped to CPUs 0–1.

```text
taskset -c 0-1 node --test --test-concurrency=1 --test-reporter=spec \
  tests/stages10-12.test.ts
3 tests passed, 0 failed

corepack pnpm typecheck
typecheck contract passed

corepack pnpm exec eslint \
  packages/protocol/src/workflow.ts \
  packages/store-sqlite/src/workflows.ts \
  packages/kernel/src/workflows.ts \
  packages/kernel/src/event-ingress.ts \
  packages/kernel/src/project-context.ts \
  apps/gateway/src/server.ts \
  tests/stages10-12.test.ts
passed

git diff --check
passed

node -e "JSON.parse(require('fs').readFileSync('packages/store-sqlite/package.json','utf8')); JSON.parse(require('fs').readFileSync('packages/kernel/package.json','utf8')); console.log('package manifests valid')"
package manifests valid
```

The final branch HEAD is recorded in the initial pull-request body.

## Explicit not-tested gaps

- The broad repository test suite was not run locally.
- Remote CI and post-merge checks are not represented by this local proof log.
- No production activation or external event source was performed.
